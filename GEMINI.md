# GEMINI.md

This file serves as the foundational mandate and project context for Gemini CLI. It synchronizes with `CLAUDE.md` to ensure consistent behavior across AI agents.

## Scope: HomeLab Infrastructure
Covers networking (OPNsense, VLANs, router-on-a-stick), virtualization (KVM/libvirt, Podman), Active Directory (DC01), and related lab work. Obsidian accesses this directory via a symlink at `~/Documents/brain/HomeLab/`.

## Core Directives
1. **Journaling:** Always log daily configuration changes, KVM/Podman troubleshooting, and major decisions in `Build-Log.md` in the project root. Review it when starting complex lab tasks.
2. **Dated entries:** Append `## YYYY-MM-DD — <topic>` sections to `Build-Log.md` after meaningful work so the other AI sees it next session.

## Context Synchronization
This project also uses `CLAUDE.md`. Any major architectural changes should be updated in both files to keep all agents in sync.

## Related
The ITD 132 SQL project moved out on 2026-04-20 — now at `~/Documents/School/ITD132/`.
